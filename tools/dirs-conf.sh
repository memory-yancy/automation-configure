#!/usr/bin/env bash

mkdir -p "${HOME}/repo" "${HOME}/github" "${HOME}/test"
mkdir -p "${HOME}/Downloads/apps-source"
mkdir -p "${HOME}/Pictures/screenshots"
mkdir -p "${HOME}/VirtualBox VMs/vm-shares"
mkdir -p "${HOME}/.mutt"
